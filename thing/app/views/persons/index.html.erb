<h1>Listing Players</h1>

<table>
  <% @persons.each do |person| %>
    <tr>
      <td><%= person.name %></td>
      <td><%= person.age %> </td>
      <td><%= link_to "Edit", edit_person_path(person) %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Home', controller: 'hello' %>

<%= link_to 'New Person', new_person_path %>

